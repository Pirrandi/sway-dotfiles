input type:keyboard {
    # Two layouts, cycled with $mod+ctrl+space (see keybindings.conf).
    # Index 0 (us) is the one active on login.
    xkb_layout "us,latam"
}

input type:touchpad {
    tap enabled
    natural_scroll enabled
    dwt enabled
}
